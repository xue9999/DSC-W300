# DSC-G3 retained input provenance

`sources/DSCG3V2.exe` and `sources/D-G3V2.dat` were present in the imported repository. Current byte identity is recorded in `artifact_manifest.json`. The parser and independent tests verify the relationship between the EXE's LHA payload and the DAT container.

The imported repository supplies both retained files. Their hashes and EXE-to-DAT relationship are reproducible locally. Treat the original acquisition note as a historical account and retain source-delivery verification as a separate provenance task.

The source executable is retained as an input, never executed. Parsing and experiments happen offline. Historical references to service manuals, FCC exhibits and OSS packages were research leads, not evidence that those materials were acquired or examined here. They are not prerequisites for the retained offline tests.

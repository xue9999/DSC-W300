# W300 portable handoff checkpoint

Editorial revision 3 of the 2026-09-16 checkpoint. Continue toward persistent English menus on the original DSC-W300, preserving identity, calibration and function. The supplied executable implements selftest, OS inventory and standard INQUIRY; its language-qualification flag remains false pending the work below.

## Completed work and next checks

| Criterion | Established foundation | Next action and verification |
|---|---|---|
| W300-specific method | Sony manual names Auto-Adj and its Service-board restriction; comparative analysis identifies PMCA transport candidates. | Inspect actual W300 code or reliable transactions; establish original-board eligibility and the exact language operation. |
| Local toolchain | PMCA startup, USB-library imports, locked wheels and offline rebuild passed. Portable release 2 passed relocation, selftest and inventory. | Acquire and inspect exact Auto-Adj/SeusEX/HASP dependencies or implement the narrowly justified W300 operation. Verify program startup and dependencies. |
| Runtime validation | Historical portable validation checked 1317 files on Windows 11; selftest and inventory exited zero. | Validate each new package after relocation, then retain reports from the eventual receiving PC. |
| Language/persistence | Manual and reference analyses distinguish fields, volatile updates and persistent writes. | Derive W300 encoding and commit behavior; confirm responses and post-restart readback. |
| First device session | Identification commands and expected identity are documented. | Follow current identity review with qualified baseline reads, affected-data backup and demonstrated restoration before the write stage. |
| Complete handoff | Instructions, reports, acquisition records and scripts are retained. | The analyst completes acquisition and protocol qualification; include all dependencies and recovery commands in the eventual write procedure. |

## Acquisition findings to build on

Inspected routes include pinned PMCA and six historical maintainer attachments, Sony's exact W300 GPL release, the full adjustment PDF and object-level locators, public software/firmware archives, IBISS and historical SeusEX catalogs, local filenames/Git objects, independent PTP source and retained G3 native/script code. The GPL source supplies USB infrastructure; pursue the proprietary W300 function/application layer using a new concrete package, firmware or transaction locator.

Further work recovered the watermark forum archive and W300 L3 listing, Sony's historical update catalogs and linked year pages, and primary owner posts. Their retained contents are manuals, catalog entries and owner observations. Follow a new binary-bearing lead, inspect its contents and model provenance, and compare operations with the precise transport questions already recorded. Reuse existing reports instead of repeating unchanged queries.

## Execution and preservation

Continue offline without connecting a camera here. Use `W300Workbench.exe selftest`, then OS `inventory` during the later receiving-PC session. Preserve identity matching, bounded INQUIRY and simulator guards. Implement service operations from W300 evidence, retain affected-data backups and prove restoration before the separately authorized language write.

See `docs/w300/EXECUTION_PLAN.md` and `VERIFICATION.md` for the procedure and evidence map. Prior machine-readable checks and the pre-editorial manifest preserve their results; this revision turns checkpoint findings into an actionable continuation guide.

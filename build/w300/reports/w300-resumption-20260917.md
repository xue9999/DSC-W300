# W300 language research: acquisition and implementation handoff

## Outcome

The original Japanese DSC-W300 has not been converted or connected during this continuation. No new W300 executable, proprietary firmware, service transaction or destination encoding was acquired. The language and hardware qualification milestones remain unverified. The existing identification workbench remains unchanged.

The external evidence gap is the missing W300 implementation or a trustworthy W300 transaction capture. This is not evidence that original-board conversion is impossible. The precise target remains `DSC-W300 Auto-Adj Ver_1.3r04.exe` and its payload, with compatible SeusEX dependencies; a provenance-qualified W300 firmware image or trace can provide an alternative.

## Checks and their limits

Two independent read-only reviews covered the execution plan, verification report, original-board/recovery audit, GPL boundary and acquisition reports. The proprietary W300 function handler is absent from the retained GPL source. Retained G3 findings do not establish W300 addresses, field values, eligibility or persistence.

Additional public searches for the exact model/program, component identifier, dump and regional firmware/adjustment terms did not yield a binary-bearing locator. Returned manuals, unrelated products and existing support pages were not treated as firmware. This is a bounded search result, not a claim about all public or private archives.

The historical Sony/libmtp response and its architecture presentation were checked against `sony-ptp-boundary-2008.md` and were already acquired. The watermark archive and historical Sony package catalog routes in older follow-up prose have likewise already been covered by `w300-watermark-archive/` and `w300-package-source/`; they are not outstanding new leads.

A single ordinary public GET to `https://grep.app/api/search?q=DSC-W300` returned HTTP 429. This continuation reused the bounded, TLS-verifying fetch helper in `w300-package-source/acquire.py`. No search response body or repository locator was acquired, and no retry or access workaround followed. The local acquisition record is `build/w300/downloads/w300-resumption-20260917/acquisition.json`. A rate-limit response does not establish absence from the index.

## Acquire the next artifact and prepare its analysis

The agent owns acquisition of the next W300-specific implementation artifact. Search a distinct service-package, firmware or transaction source using the retained coverage to avoid duplicates; inspect available archive inventories and follow concrete attachment links. If a source provides no usable lead, move to another route in the [execution plan](../../../docs/w300/EXECUTION_PLAN.md). In parallel, prepare the bounded file-transfer qualification described below. For each acquired artifact:

1. Preserve the original bytes and source context; inspect its archive, embedded model/version and dependencies without running the program. A filename or PID match alone is insufficient.
2. Start with the documented **Destination Check** read path, then trace its SeusEX calls, response validation and destination representation.
3. Follow the original-board/service-board condition and the exact write path. The manual's service-board restriction remains unresolved for the user's original board.
4. Identify the narrowest language-only update if available. If destination conversion is necessary, establish all affected fields and actual encoding. The manual's U2 (English/NTSC) and CEE8 (English/PAL) labels are not raw write values.
5. Establish affected-data backup, restoration, commit and post-restart readback before preparing a hardware write procedure. SERIAL and ADJBAK files have distinct documented scopes and do not establish complete recovery coverage.

The program revision matters: the retained Sony manual records a Destination Data Write correction in 1.2r03 and an Adjustment Mode entry correction in 1.3r04. Earlier versions require comparison rather than silent substitution. See `service-board-audit.md` for source pages and operation details.

## Continue across routes

Subsequent offline inspection established an additional acquisition candidate: [G3 path-based FileControl reads](g3-file-read/README.md). This narrows the earlier dependency on an externally obtained package: actual W300 code might instead be acquired from the owner's camera after separately qualifying its service lifecycle and transfer. The finding does not qualify W300 or change the user's current no-camera constraint.

Continue package acquisition by locating a distinct file, attachment, repository reference, archive inventory or accessible service source; obtaining that lead is part of the task. If a particular source is exhausted, record its coverage and select another route without waiting for the user to supply a package. Keep comparative G3 analysis tied to a specific W300 acquisition or implementation question and label its model scope.

Without a connected camera, advance the file-read route by resolving the retained G3 USB padding/segment behavior, specifying bounded receiver checks and preparing an evidence-based entry/exit qualification checklist. These tasks prepare a later W300 session without claiming it has occurred. The receiving-PC inventory/INQUIRY session supplies identity evidence; qualify service transport and persistence separately. Report the whole task as unable to advance only after assessing the remaining routes and identifying the concrete external input each needs, together with all useful preparation already completed.
